name: phase01
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
    
  # State
  bloc: ^8.0.3
  flutter_bloc: ^8.0.1
  
  #Database
  sqflite: ^2.2.4+1

  # io-utils
  path_provider: ^2.0.14

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
